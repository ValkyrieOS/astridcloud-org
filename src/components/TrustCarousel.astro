---
const logos = [
    "/4ea5a3642c5e9a7a6c16d10a30e9b270.webp",
    "/50973423826112eb5c9bf50d4b07f4d7.webp",
    "/4ea5a3642c5e9a7a6c16d10a30e9b270.webp",
    "/50973423826112eb5c9bf50d4b07f4d7.webp",
    "/4ea5a3642c5e9a7a6c16d10a30e9b270.webp",
    "/50973423826112eb5c9bf50d4b07f4d7.webp",
];
---

<section class="py-20 bg-neutral-900/50 overflow-hidden">
    <div class="max-w-7xl mx-auto px-6 mb-12 text-center">
        <h2 class="text-3xl sm:text-4xl font-bold text-white mb-4">
            Los que confían en nosotros
        </h2>
    </div>

    <div class="relative flex overflow-x-hidden group">
        <div class="animate-marquee whitespace-nowrap flex items-center">
            {
                logos.map((logo) => (
                    <div class="mx-12 w-32 h-32 flex items-center justify-center grayscale hover:grayscale-0 transition-all duration-300 opacity-70 hover:opacity-100">
                        <img
                            src={logo}
                            alt="Partner logo"
                            class="max-w-full max-h-full object-contain"
                        />
                    </div>
                ))
            }
        </div>

        <div
            class="absolute top-0 animate-marquee2 whitespace-nowrap flex items-center"
        >
            {
                logos.map((logo) => (
                    <div class="mx-12 w-32 h-32 flex items-center justify-center grayscale hover:grayscale-0 transition-all duration-300 opacity-70 hover:opacity-100">
                        <img
                            src={logo}
                            alt="Partner logo"
                            class="max-w-full max-h-full object-contain"
                        />
                    </div>
                ))
            }
        </div>
    </div>
</section>

<style>
    .animate-marquee {
        animation: marquee 25s linear infinite;
    }
    .animate-marquee2 {
        animation: marquee2 25s linear infinite;
    }
    @keyframes marquee {
        0% {
            transform: translateX(0%);
        }
        100% {
            transform: translateX(-100%);
        }
    }
    @keyframes marquee2 {
        0% {
            transform: translateX(100%);
        }
        100% {
            transform: translateX(0%);
        }
    }
</style>
